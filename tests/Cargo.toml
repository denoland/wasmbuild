[package]
name = "deno_test"
version = "0.1.0"
edition = "2021"
authors = ["the Deno authors"]
license = "MIT"

[features]
start = []

[dependencies]
wasm-bindgen = "0.2.90"

[lib]
name = "deno_test"
crate-type = ["cdylib"]
path = "lib.rs"
